<%= form_with(model: reply, local: true) do |form| %>
  <% if reply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reply.errors.count, "error") %> prohibited this reply from being saved:</h2>

      <ul>
      <% reply.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :reply_message %>
    <%= form.text_field :reply_message, id: :reply_reply_message %>
  </div>

  <div class="field">
    <%= current_user.email %>
    <!-- <%= form.text_field :user_id, id: :reply_user_id %> -->
  </div>

  <div class="field">
    <%= form.label :tweet_id %>
    <%= form.text_field :tweet_id, id: :reply_tweet_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
